(function() {
    angular.module('app')
        .config([
            '$httpProvider', '$provide', function ($httpProvider, $provide) {
            }
        ])
        .run([
            '$rootScope', function ($rootScope) {
            }
        ]);
})();